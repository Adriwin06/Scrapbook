{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Scrapbook (Debug)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/Debug/scrapbook.exe",
      "cwd": "${workspaceFolder}/build/bin/Debug",
      "preLaunchTask": "Build + Deploy (Debug)",
      "console": "integratedTerminal"
    }
  ]
}
